# Example environment variables (DO NOT commit real secrets)
# Copy to `.env` locally and fill in real values, or set them in your
# deployment provider (Render/Vercel) environment settings.

MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/gs-co-products
PORT=6969
ADMIN_SECRET=your-admin-secret
# Frontend build-time API URL for Vite (set in frontend deploy or local .env)
VITE_API_URL=https://gs-co-backend.onrender.com/api
